apiVersion: v1
kind: Service
metadata:
  name: booking-service
  namespace: smart-resource
  labels:
    app: booking-service
spec:
  type: ClusterIP
  ports:
  - port: 5003
    targetPort: 5003
    protocol: TCP
  selector:
    app: booking-service
