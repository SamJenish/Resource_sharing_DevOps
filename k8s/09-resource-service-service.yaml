apiVersion: v1
kind: Service
metadata:
  name: resource-service
  namespace: smart-resource
  labels:
    app: resource-service
spec:
  type: ClusterIP
  ports:
  - port: 5002
    targetPort: 5002
    protocol: TCP
  selector:
    app: resource-service
