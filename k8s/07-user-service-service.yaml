apiVersion: v1
kind: Service
metadata:
  name: user-service
  namespace: smart-resource
  labels:
    app: user-service
spec:
  type: ClusterIP
  ports:
  - port: 5001
    targetPort: 5001
    protocol: TCP
  selector:
    app: user-service
