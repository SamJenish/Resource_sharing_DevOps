apiVersion: v1
kind: Service
metadata:
  name: frontend
  namespace: smart-resource
  labels:
    app: frontend
spec:
  type: NodePort
  ports:
  - port: 3000
    targetPort: 3000
    nodePort: 30000
    protocol: TCP
  selector:
    app: frontend
