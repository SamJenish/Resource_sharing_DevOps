apiVersion: v1
kind: Service
metadata:
  name: api-gateway
  namespace: smart-resource
  labels:
    app: api-gateway
spec:
  type: ClusterIP
  ports:
  - port: 5000
    targetPort: 5000
    protocol: TCP
  selector:
    app: api-gateway
